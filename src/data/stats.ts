import { Stat } from "@/types/experience";

/**
 * Estadísticas clave (deben ser reales y verificables, nunca infladas).
 * Ejemplo de forma:
 * { id: "stat-1", label: "Proyectos completados", value: 5, suffix: "+" }
 */
export const stats: Stat[] = [];
